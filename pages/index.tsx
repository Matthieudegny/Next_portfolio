import Link from "next/link";
import Image from "next/image";
import Head from "next/head";
import { motion } from "framer-motion";
import { useRouter } from "next/router";
import { container, item } from "../animation";
//components
import LayoutText from "../components/LayoutText";
import { useEffect } from "react";

import { dataSkills } from "./utils/skillsData/skillsData";
import { useState } from "react";

type test = {
  title: string;
  image: string | any;
  text: string;
};

export default function Home({ projects }: { projects: any }) {
  const [offsetY, setoffsetY] = useState(250);
  const [offsetX, setoffsetX] = useState(205);
  const router = useRouter();

  let indiceRotationX = (offsetY - 250) / -12.5;
  let indiceRotationY = (offsetX - 210) / 10.5;

  useEffect(() => {
    console.log("offsetY", offsetY);
  }, [offsetY]);

  const test: test = {
    title: dataSkills[0].title,
    image: dataSkills[0].linkImage,
    text: dataSkills[0].text,
  };

  return (
    <motion.main
      animate={{ y: "0%" }}
      exit={{ opacity: 1 }}
      initial={{ y: "100%" }}
      transition={{
        duration: 0.75,
        ease: "easeOut",
      }}
      className="text-gray-900 absolute top-0 left-0 w-full min-h-full bg-orange-400 overflow-y-auto"
    >
      <Head>
        <title>PortFolio</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className=" relative min-h-screen w-screen flex flex-col justify-center bg-orange-400 lg:px-48 px-16  ">
        <div className="w-3/5 h-40v mt-8 text-4xl font-playfair">
          <LayoutText delay={1} lineHeight={5} timeAnimation={0.08}>
            Hello, I'm Matthieu, a front-end developer specialized, with modern
            technologies built around the Javascript framework.
          </LayoutText>
        </div>

        <div className="min-w-full h-40v mt-8 pl-80 text-3xl">
          <LayoutText delay={1} lineHeight={4} timeAnimation={0.05}>
            As a front-end developer with expertise in React, Next.js, and
            TypeScript, I'm passionate about crafting user-friendly interfaces
            that bring ideas to life.
          </LayoutText>
        </div>

        <div className="text-3xl mb-20">What i can bring to you:</div>

        <article className="flex justify-evenly  min-h-40v ">
          <main className=" flex flex-col justify-center -mt-4  w-6/12">
            <h3 className="text-3xl mb-4 ml-4">{test.title}:</h3>
            <h4 className=" text-2xl leading-10">{test.text}</h4>
          </main>
          <div
            className=" relative "
            style={{
              perspective: "1000px",
              perspectiveOrigin: "50% 50%",
              transform: "rotate3d(0, 0.8, 0, 34deg)",
              width: "500px",
              height: "500px",
            }}
            onMouseMove={(event) => {
              setoffsetY(event.nativeEvent.offsetY);
              setoffsetX(event.nativeEvent.offsetX);
            }}
            onMouseLeave={() => {
              setoffsetY(250);
              setoffsetX(205);
            }}
          >
            <img
              src={test.image}
              alt="my_image"
              style={{
                borderRadius: "10px",
                transform: `rotateX(${indiceRotationX}deg) rotateY(${indiceRotationY}deg)`,
                transition: "all 0.5s ease",
              }}
            />
          </div>
        </article>

        <div className="min-w-full h-10v flex items-end justify-end">
          <div className="">
            ©2023 <span>Available for work from July</span>
          </div>
        </div>
      </main>
    </motion.main>
  );
}

// export async function getStaticProps() {
//   const projects = await client.fetch(`*[_type == "projects"]`);

//   return {
//     props: {
//       projects,
//     },
//   };
// }
